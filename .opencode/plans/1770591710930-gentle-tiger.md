# Plan: Upgrade Node 22 to Node 24

## Change

One file: `.github/workflows/test.yml` line 49

```yaml
# Before
node-version: '22'

# After
node-version: '24'
```

This is the only remaining Node 22 reference in the project. `version.yml` already uses Node 24.

## Verification

No code verification needed — this is a CI config change only.
