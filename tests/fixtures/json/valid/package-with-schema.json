{
  "$schema": "https://json.schemastore.org/package.json",
  "name": "test-package-with-schema",
  "version": "1.0.0",
  "description": "A test package with schema reference",
  "main": "index.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "build": "echo \"No build process\"",
    "start": "node index.js"
  },
  "keywords": ["test", "example"],
  "author": "Test Author",
  "license": "MIT",
  "dependencies": {
    "express": "^4.18.0"
  },
  "devDependencies": {
    "typescript": "^5.0.0"
  },
  "engines": {
    "node": ">=18.0.0"
  },
  "repository": {
    "type": "git",
    "url": "https://github.com/example/test-package.git"
  }
}