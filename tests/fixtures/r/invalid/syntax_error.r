# R script with syntax errors

# Missing closing parenthesis
hello <- function(name {
  return(paste("Hello", name))
}

# Undefined function call
result <- undefined_function(42)

# Mismatched brackets
data <- c(1, 2, 3, 4, 5]]

# Invalid assignment
123abc <- "invalid variable name"

# Unclosed string
message <- "This string is not closed

print(message)