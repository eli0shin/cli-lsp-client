type Query {
  user(id: ID!): User
  users: [User!]!
}

type User {
  id: ID!
  name: String!
  email: String!
  posts: [Post!]!
  invalidField: NonExistentType  # This should cause an error
}

type Post {
  id: ID!
  title: String!
  content: String!
  author: User!
  publishedAt: String
}