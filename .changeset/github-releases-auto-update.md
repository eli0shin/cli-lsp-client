---
'cli-lsp-client': minor
---

Add GitHub Releases binary publishing, install script for direct binary installation via curl, and auto-update system that checks for new versions in the background and stops all daemons after updating
